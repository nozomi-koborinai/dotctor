[package]
name = "dotctor"
version = "0.1.0"
edition = "2024"
description = "A CLI tool to diagnose your dotfiles health"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }
